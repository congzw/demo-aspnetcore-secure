@page
@using CookieSample.Demo
@inject ILockUserManager LockUserManager
@{
    var userName = "admin@demo.com";
    if (!string.IsNullOrWhiteSpace(userName))
    {
        LockUserManager.SetLock(userName, false);
    }
}
<h1>Unlocked for: @userName</h1>